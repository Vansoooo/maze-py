# Maze-Py 

Вдохновлено проектом [emilco/pathfinder](https://github.com/emilco/pathfinder), адаптированным для лабораторных работ студентов.

## Цель проекта

Научиться:
- **Реализовывать** классические алгоритмы поиска пути
- **Понимать** различия между алгоритмами
- **Визуализировать** работу алгоритмов
- **Сравнивать** эффективность разных подходов

## Установка и запуск

### 1. Установка
```bash
# Клонируйте репозиторий
git clone <репозиторий>
cd maze_py

# Создайте виртуальное окружение
python -m venv .venv

# Активируйте его
# На Linux/Mac:
source .venv/bin/activate
# На Windows:
# .venv\Scripts\activate

# Установите зависимости
pip install -e .
```

### 2. Быстрый старт
```bash
# Запустите с алгоритмом BFS
python main.py --width 20 --height 15 --solver bfs

# Сгенерируйте лабиринт и экспортируйте для визуализации
python main.py --width 40 --height 20 --seed 123 --export-json web/data/latest.json
```

## Что нужно реализовать

Проект содержит базовую инфраструктуру, но **вам предстоит реализовать**:

### 🔍 Алгоритмы поиска пути (ЗАДАНИЕ)

1. **Depth-First Search (DFS)** - поиск в глубину
   - Использует **стек** (LIFO)
   - Не гарантирует кратчайший путь
   - Прост в реализации

2. **Dijkstra's Algorithm** - алгоритм Дейкстры
   - Находит кратчайший путь во **взвешенном графе**
   - Использует **приоритетную очередь**
   - Учитывает веса рёбер

### Архитектура проекта

```
maze_py/
├── grid.py             # Ядро: Cell, MazeGrid, MazeTree
├── generators/         # Алгоритмы генерации лабиринтов
├── solvers/            #  ВАША РАБОЧАЯ ОБЛАСТЬ!
│   ├── base.py         # Базовый класс MazeSolver
│   ├── bfs.py          # Готовый пример BFS
│   ├── dfs.py          # ЗДЕСЬ РЕАЛИЗУЙТЕ DFS!
│   └── dijkstra.py     # ЗДЕСЬ РЕАЛИЗУЙТЕ ДЕЙКСТРУ!
├── animation.py        # Запись анимации
└── renderers/          # Визуализация
```

## Как добавить свой алгоритм

### Шаг 1: Создайте класс-наследник

Создайте новый файл в `maze_py/solvers/`, например `dfs.py`:

```python
"""Реализация поиска в глубину (DFS)."""

from __future__ import annotations
from typing import List, Tuple

from ..animation import AnimationRecorder
from ..grid import Cell, MazeTree
from .base import MazeSolver


class DepthFirstSolver(MazeSolver):
    """Вам нужно реализовать этот класс."""
    
    name = "dfs"  # Уникальное имя для CLI
    
    def _traverse(
        self,
        tree: MazeTree,
        target: Tuple[int, int],
        *,
        recorder: AnimationRecorder | None = None,
    ) -> Tuple[List[Cell], List[Cell]]:
        """
        Реализуйте DFS!
        
        Подсказки:
        1. Используйте стек вместо очереди
        2. Для стека в Python можно использовать list
        3. stack.pop() удаляет последний элемент
        4. stack.append() добавляет в конец
        """
        # TODO: Ваша реализация здесь
        pass
```

### Шаг 2: Зарегистрируйте алгоритм

Добавьте в `maze_py/solvers/__init__.py`:

```python
"""Solver exports."""

from .breadth_first import BreadthFirstSolver
from .depth_first import DepthFirstSolver
from .dijkstra import DijkstraSolver
from .results import MazeSolution, SolverStats

__all__ = [
    "BreadthFirstSolver",
    "DepthFirstSolver", # Ваше 
    "DijkstraSolver", # Ваше
    "MazeSolution",
    "SolverStats",
]
```

### Шаг 3: Тестируйте!

```bash
# Запустите ваш алгоритм
python main.py --width 30 --height 20 --solver dfs

# Сравните с BFS
python main.py --width 30 --height 20 --solver bfs
```

## Визуализация работы

### Веб-визуализатор

1. Экспортируйте данные:
```bash
python main.py --width 40 --height 20 --export-json web/data/latest.json
```

2. Запустите сервер:
```bash
cd web
python -m http.server 8000
```

3. Откройте в браузере: `http://localhost:8000`

## Полезные команды

```bash
# Справка по аргументам
python main.py --help

# Генерация конкретного лабиринта
python main.py --width 30 --height 20 --seed 42

# Разные алгоритмы генерации
python main.py --generator spanning-tree
python main.py --generator binary-tree
python main.py --generator random-growth

# Сохранение результата
python main.py --width 40 --height 20 --export-json my_maze.json
```

## Частые вопросы

### "Где взять документацию по базовым классам?"
Смотрите код в:
- `solvers/base.py` - интерфейс MazeSolver
- `grid.py` - Cell, MazeGrid, MazeTree
- `animation.py` - запись шагов

### "Как понять, правильно ли работает мой алгоритм?"
1. Проверьте, что путь существует (соединяет старт и цель)
2. Сравните с BFS на простых лабиринтах
3. Используйте визуализатор


## Задачи для продвинутых

После реализации базовых алгоритмов:

1. **Добавьте A*** алгоритм
2. **Реализуйте двунаправленный поиск**
3. **Добавьте эвристики** для улучшения производительности
4. **Создайте сравнительную таблицу** алгоритмов
